# Bundled upstream songs were removed (copyright risk).

This project ships royalty-free beds in ../../resource/songs/ (copied here by
`make vendor` and mounted into Docker). Set `bgm_type: random` in
topics/queue.yaml to use them.
